# Generate a clean onboarding checkout

No prepared `.ramp` snapshot is shipped. Generate a fresh checkout with `onboard` as documented in START_HERE.md. It contains the installed guidance and no prior task or solution. The agent writes the task specification during the session.

Historical snapshots were intentionally removed from the release manifest; generated task evidence belongs to its actual run.
