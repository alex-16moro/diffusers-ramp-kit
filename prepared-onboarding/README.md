# The engineer clones the prepared fork branch

No prepared `.ramp` task snapshot is shipped. The platform owner prepares and publishes the reviewed fork `ramp-base` using `onboard`/`attach` on a separate host (RELEASE.md). The engineer clones only that branch, bootstraps and opens it in Cursor (START_HERE.md). The engineer host must not contain this kit repository or its completed solutions. The agent authors its own task specification during the clean session.
